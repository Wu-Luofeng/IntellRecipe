worker_processes  1;

events {
    worker_connections  1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    upstream intellrecipe_gateway {
        # Gateway 跑在宿主机 jar 上；Nginx 用 bridge 访问 host.docker.internal 在部分云主机上会卡死→504。
        # 配合下方 docker-compose 中 nginx 的 network_mode: host，此处连本机 Gateway。
        server 127.0.0.1:10010;
    }

    server {
        listen       80;
        server_name  _;

        location / {
            root   /usr/share/nginx/html;
            index  index.html index.htm;
        }

        # 代理后端 API（默认 60s 读超时易 504；Gateway→微服务链略长时放宽）
        location /api/ {
            proxy_pass http://intellrecipe_gateway/;
            proxy_connect_timeout 15s;
            proxy_send_timeout 120s;
            proxy_read_timeout 120s;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}